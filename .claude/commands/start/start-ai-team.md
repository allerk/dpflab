Verify if environmental variable `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` value is `1`. If not, then return error.

The `Agents` team roster is located in `@.claude/agents/` Use TeamCreate tool to create a tema and register those agents to the teams.

Read `@.claude/common-prompt.md` file to consume general rules and requirements which applies to all agents, including the
team-lead.